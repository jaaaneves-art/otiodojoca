# OTJ — Plano de sessão

- Data/hora: 15/09/2026, 20:10, Europe/Lisbon (WEST, UTC+1).
- Estado: **continuação da sessão aberta desde 13/09/2026 09:45**
  (secção 6 das instruções: mudança de dia não fecha sessão).
  Não substitui os planos anteriores (13/09 09:45 e 14/09 12:09);
  reconcilia-os.

## Ambiente e limitação técnica desta sessão

Trabalho sobre uma **cópia extraída de um ZIP** (o utilizador enviou o
projeto em 3 partes: `otiodojoca.parte001/2/3`):

- Pasta de trabalho: `/mnt/agents/output/otiodojoca/otiodojoca`
  (**não é** o `~/Nextcloud/Projectos/otiodojoca` real — alterações aqui
  não chegam ao computador do Yos sem reenvio explícito).
- **Sem pasta `.git` no ZIP** → branch (`fix/lup-rpc-20260913`) e commit
  (`616155b`) assumidos pelos registos; não verificáveis por `git`.
- **Sem shell do projeto**: não há aqui `npm`, `psql`, Supabase CLI nem
  Supabase local. Lint, testes, build, migrations e o guião de teste do
  Marketplace dependem do terminal do Yos. Mesma limitação das sessões
  Cowork anteriores (13/09 21:10–22:10 e 14/09).

## Estado de partida (reconciliado com sessions + pendentes + planos)

### Fechado e validado — não repetir

- Segurança P0 (13 Set): `SUPABASE_SECRET_KEY` rodada, TRUNCATE revogado,
  naming de 260 policies padronizado (commit `6593ab6`, push feito).
- LUP RPC-only/idempotência: concluído (testes focados 17/17, E2E local
  `LUP_E2E_OK`). Pendência não bloqueante: fotografia/anúncio não são
  transação única.
- Empregos RPC-only: concluído (E2E 10/10, ciclo completo de estados).
- Commit `616155b` (LUP + Empregos, 25 ficheiros): feito a 13 Set.

### Verificado em disco nesta sessão (cópia ZIP)

- ✅ Migration `20260913232000_marketplace_ad_completo_rpc.sql` presente
  (define `marketplace_ad_criar_completo` / `_editar_completo`).
- ✅ As 8 páginas `novo`/`editar` dos 4 módulos Marketplace presentes.
- ✅ `supabase/tests/security/rls-policies-block.test.sql` já não existe
  (pendência "apagar fisicamente" — resolvida).
- ✅ `app/mercado-da-terra/actions.ts` (2550 bytes) **não é importado por
  nenhum ficheiro** de `app/`, `components/` ou `lib/` → candidato a
  apagar (só com confirmação do Yos).

### 🚩 Discrepância confirmada (tarefa 1 da sessão)

As 8 páginas **fazem INSERT/UPDATE direto em `marketplace_ads`**
(`gran-bazar/novo` linha 72, etc.) e **zero referências** a
`marketplace_ad_criar_completo`/`_editar_completo` existem em
`app/`, `components/` ou `lib/`. Isto **contradiz** o plano de retomada
de 14/09 ("8 páginas alteradas para chamar a RPC nova, confirmado por
data de modificação").

Duas hipóteses:
- **H1** — o ZIP é mais antigo que a correção; o projeto real em
  Nextcloud já tem as páginas migradas. (Datas de modificação originais
  perderam-se na extracção; não é possível distinguir daqui.)
- **H2** — a correção das páginas nunca chegou ao disco neste estado.

Impacto se H2 for verdade no projeto real: com as policies RPC-only
ativas (`20260913180000`), criar/editar anúncios nestes 4 módulos falha
em runtime — exatamente o bug diagnosticado a 13/09.

## Tarefas prioritárias

1. ✅ Verificar como as 8 páginas gravam — FEITO (ver acima).
2. ⬜ O Yos confirma H1 ou H2 no projeto real
   (grep rápido: `grep -rl "completo" app/` em
   `~/Nextcloud/Projectos/otiodojoca`).
3. ⬜ Se H2: aplicar a correção às 8 páginas (chamar as RPC novas) — a
   migration SQL já existe; referência de design em
   `docs/pendentes/20260913T2119-correcao-marketplace-4-modulos.md`.
4. ⬜ Executar o guião de teste (`docs/pendentes/20260913T2200-guiao-
   teste-marketplace-4-modulos.md`) no terminal do Yos.
5. ⬜ `npm run lint`, `npx tsc --noEmit`, `npm run build`.
6. ⬜ Só depois: commit da lista de ficheiros do guião (sem `outputs/`).

## Validações previstas

- Guião de teste manual dos 4 módulos (criar/editar/leilão/ownership)
  no Supabase local — no terminal do Yos.
- Lint + tsc + build com registo em `outputs/`.

## Restrições mantidas

- Sem `push`, sem `db push`, sem reset destrutivo sem autorização
  explícita do Yos.
- Não repetir migrations, auditorias ou testes já aprovados.
- Não inventar estado Git — a cópia ZIP não permite verificá-lo.

## Próximo passo concreto

O Yos corre `grep -rl "completo" app/` no projeto real e diz qual das
hipóteses (H1/H2) é verdade. Consoante a resposta: confirmar e seguir o
guião de teste (H1), ou aplicar a correção às páginas aqui e preparar
os ficheiros para devolução (H2).

---

**Classificação da fase:** Evolução (verificação com evidência em
disco) sobre uma situação de **Bloqueio** (discrepância de versão entre
registos e cópia ZIP — depende de verificação no projeto real).
