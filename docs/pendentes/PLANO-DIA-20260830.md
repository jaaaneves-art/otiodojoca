# Plano do dia — 30/08/2026 07:48

Baseado no relatório de ontem (`claude/RELATORIO-COMPLETO-20260829.md`) e nos pendentes registados em `docs/pendentes/`. Ordem sugerida: os três pendentes de ontem primeiro (por esta ordem de prioridade), depois continuação da metodologia do módulo social, depois os itens de baixa prioridade se sobrar tempo.

## 1. Netuno e códigos postais — Prioridade 1

**Detalhe:** `docs/pendentes/NETUNO-CODIGOS-POSTAIS-20260829.md`.

- Confirmar com o Yos o que recorda sobre a ligação Netuno ↔ códigos postais (que ferramenta era, quando foi usada, se ainda corre fora deste repositório).
- Correr a query já preparada para perceber se `arteria`, `codigo_postal` e `codigo_postal_arteria` (duplicadas, tamanhos diferentes) são schemas diferentes ou dados duplicados.
- Decidir: manter ou remover as 16 tabelas `netuno_*` restantes (com backup/dump antes de qualquer `DROP TABLE`).
- Resolver as tabelas de códigos postais duplicadas — liberta mais de 100 MB na base de dados (ainda perto do limite do plano Free depois do `TRUNCATE` de ontem).

## 2. StandGo: reforçar com o AutoNex + escolher novo nome — Prioridade 2

**Detalhe:** `docs/pendentes/STANDGO-REFORCO-AUTONEX-RENOME-20260829.md`.

- Se a sessão não tiver continuidade do anexo original, pedir ao Yos para reenviar o `AutoNexcompleto.zip` (o prompt/especificação já está gravado no documento, mas o código só existia no anexo).
- Usar o AutoNex como referência de UX (autocomplete de marca/modelo, filtros avançados, mapa Leaflet) a integrar no StandGo real (Next.js/Supabase) — não copiar como site à parte.
- Levar uma short-list de nomes alternativos a "StandGo" para discutir com o Yos (nada decidido ainda).

## 3. JobNex: analisar e decidir destino — Prioridade 3

**Detalhe:** `docs/pendentes/JOBNEX-ANALISE-20260830.md`.

- Perguntar ao Yos: módulo novo no OTJ (bolsa de emprego local) ou só inspiração de UX, sem intenção de construir já?
- Se avançar: definir nome do módulo e se liga a entidades/freguesias/municípios já existentes.
- Lembrete: trocar os nomes de empresas reais (Farfetch, OutSystems, Unbabel, Feedzai, Talkdesk) por fictícios antes de qualquer publicação, mesmo que seja só demo.

## 4. Continuação da metodologia do módulo social (prompt mestre, 13 fases)

Depois dos três pendentes acima, ou em paralelo se o Yos preferir — próximo passo natural do roteiro:

- **FASE 3** — testes de RLS das tabelas novas do módulo social (`conversations`, `messages`, `groups`, `call_rooms`, etc.), aplicadas ontem na Fase 2.
- **FASE 4** — implementação de mensagens no código.
- **FASE 5** — confirmação dos buckets de Storage.

## 5. Baixa prioridade — só se sobrar tempo

- `docs/pendentes/DB-DIFF-DECLARATIVO-NAO-CONFIGURADO-20260829.md` — configurar `schema_paths` no `config.toml` para o `db diff` voltar a ser utilizável. Não bloqueia nada; migrations continuam seguras à mão.
- Política de retenção de media (`message_media.expires_at`, secção 17 do prompt mestre) — decide se o beta corre perto de custo zero (ver `claude/ESTUDO-CUSTO-ZERO-BETA-3-MESES-20260829.md`).
- Confirmar CAE-uso do StandGo "Ceder" e outros itens menores de `claude/RELATORIO-COMPLETO-20260828.md`.

## Parado, sem ação hoje

- `docs/pendentes/OAUTH-SOCIAL-LOGIN-20260828.md` — Google/Facebook login já implementado e testado, só falta ativar no Supabase Dashboard. Decisão do Yos: só configurar quando o domínio definitivo estiver escolhido. Nenhuma ação necessária a não ser que o domínio seja decidido hoje.
